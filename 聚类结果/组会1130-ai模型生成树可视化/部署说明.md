# Dashboard 部署说明

## 📦 压缩包内容

`dashboard.zip` 包含了完整的AI创新机制仪表板项目，已排除 `node_modules`（可通过 `npm install` 重新安装）。

**✅ 完全支持 Windows、macOS、Linux 系统**

## 🚀 快速开始

### Windows 用户

#### 1. 解压文件
- 右键点击 `dashboard.zip`，选择"解压到当前文件夹"或"全部提取"
- 或者使用 PowerShell：
  ```powershell
  Expand-Archive dashboard.zip
  cd dashboard
  ```

#### 2. 安装依赖
打开 PowerShell 或 CMD，进入 dashboard 目录：
```powershell
cd dashboard
npm install
```

如果网络较慢，可以使用国内镜像：
```powershell
npm install --registry=https://registry.npmmirror.com
```

#### 3. 启动开发服务器
```powershell
npm run dev
```

服务器会自动启动，浏览器会自动打开 http://localhost:3000

#### 4. 构建生产版本（可选）
```powershell
npm run build
```

---

### macOS / Linux 用户

#### 1. 解压文件
```bash
unzip dashboard.zip
cd dashboard
```

#### 2. 安装依赖
```bash
npm install
```

如果网络较慢，可以使用国内镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

#### 3. 启动开发服务器
```bash
npm run dev
```

服务器会自动启动，访问 http://localhost:3000

#### 4. 构建生产版本（可选）
```bash
npm run build
```

---

### 构建产物部署

构建完成后，文件在 `dist/` 目录，可以部署到任何静态服务器：
- **Windows**: IIS、Nginx
- **macOS/Linux**: Nginx、Apache
- **云平台**: Vercel、Netlify、GitHub Pages

## 📁 项目结构

```
dashboard/
├── public/
│   └── data/
│       ├── dashboard_data.json    # 图表数据
│       └── evolution_tree.json    # 演化树数据
├── src/
│   ├── components/
│   │   ├── charts/                # 5个图表组件
│   │   └── DashboardLayout.jsx   # 布局组件
│   ├── App.jsx                    # 主应用
│   └── main.jsx                   # 入口文件
├── package.json                   # 依赖配置
├── vite.config.js                 # Vite配置
├── index.html                     # HTML模板
├── README.md                      # 项目说明
└── QUICKSTART.md                  # 快速启动指南
```

## 📊 功能特性

- ✨ 5个交互式图表
- 🎨 深色主题设计
- 📱 响应式布局
- 🖱️ 完整的交互功能
- 📈 实时数据可视化

## ⚙️ 系统要求

- **Node.js 16+** （所有平台）
- **npm** 或 **yarn**（所有平台）

### Windows 用户安装 Node.js
1. 访问 https://nodejs.org/
2. 下载 Windows 安装包（.msi）
3. 运行安装程序，按提示完成安装
4. 打开 PowerShell 或 CMD，验证安装：
   ```powershell
   node --version
   npm --version
   ```

## 🔧 常见问题

### Q: npm install 失败？
**Windows用户**：
- 确保以管理员身份运行 PowerShell 或 CMD
- 尝试使用国内镜像：`npm install --registry=https://registry.npmmirror.com`
- 检查防火墙是否阻止了npm

**所有平台**：
- 检查网络连接
- 清除npm缓存：`npm cache clean --force`

### Q: 页面显示"加载错误"？
A: 检查 `public/data/` 目录下的JSON文件是否存在

### Q: Windows 上端口被占用？
A: 修改 `vite.config.js` 中的端口号，或关闭占用3000端口的程序

### Q: 如何更新数据？
A: 替换 `public/data/dashboard_data.json` 和 `evolution_tree.json` 文件即可

### Q: Windows PowerShell 执行策略错误？
A: 运行以下命令允许脚本执行：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## 📝 注意事项

- 压缩包已排除 `node_modules`，需要运行 `npm install` 安装依赖
- 数据文件位于 `public/data/` 目录
- 开发服务器默认运行在 http://localhost:3000

